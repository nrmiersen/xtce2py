"""Package: {{ package_name }}."""

{% if subpackages %}
from . import (
    {% for sub in subpackages %}
    {{ sub }},
    {% endfor %}
)
{% endif %}

{% if commands %}
from . import cmd
from .cmd import (
    {% for cls in commands %}
    {{ cls }},
    {% endfor %}
)
{% endif %}

{% if telemetry %}
from . import tlm
from .tlm import (
    {% for cls in telemetry %}
    {{ cls }},
    {% endfor %}
)
{% endif %}

__all__ = [
    {% for sub in subpackages %}
    "{{ sub }}",
    {% endfor %}
    {% if commands %}
    "cmd",
    {% endif %}
    {% for cls in commands %}
    "{{ cls }}",
    {% endfor %}
    {% if telemetry %}
    "tlm",
    {% endif %}
    {% for cls in telemetry %}
    "{{ cls }}",
    {% endfor %}
]