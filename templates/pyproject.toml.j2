[project]
name = "{{ dist_name }}"
version = "{{ version }}"
description = "Python package generated from XTCE space system: {{ space_system_name }}"
dependencies = [
    "pydantic>=2.11.7",
]
requires-python = ">=3.11"
readme = "README.md"
license = { text = "MIT" }